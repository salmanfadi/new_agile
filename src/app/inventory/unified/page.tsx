import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function UnifiedInventoryPage() {
  return (
    <div className="container mx-auto py-6">
      <Card>
        <CardHeader>
          <CardTitle>Inventory View</CardTitle>
        </CardHeader>
        <CardContent>
          <p>This inventory view has been removed. Please use the inventory section in the enhanced inventory page.</p>
        </CardContent>
      </Card>
    </div>
  );
}
